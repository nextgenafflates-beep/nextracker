{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack pnpm --dir apps/web install --frozen-lockfile && corepack pnpm --dir apps/web build",
  "outputDirectory": "apps/web/.next"
}


